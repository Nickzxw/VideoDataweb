# 应用名称
Spring:
  application:
    # 数据库驱动：
    name: dataweb
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:5555/vgdata?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&failOverReadOnly=false
    username: root
    password: 123456
# 应用服务 WEB 访问端口
server:
  port: 830
mybatis:
  mapper-locations: classpath*:mapper/*.xml

